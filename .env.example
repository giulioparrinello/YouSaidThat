# ─── Database (Supabase PostgreSQL) ──────────────────────────────────────────
DATABASE_URL=postgresql://postgres.[project-ref]:[password]@aws-0-eu-central-1.pooler.supabase.com:6543/postgres

# ─── OpenTimestamps calendar servers ─────────────────────────────────────────
OTS_CALENDAR_URL_1=https://alice.btc.calendar.opentimestamps.org
OTS_CALENDAR_URL_2=https://bob.btc.calendar.opentimestamps.org

# ─── RFC 3161 TSA (Actalis, free tier, no registration needed for low volume) ─
TSA_URL=https://tsa.actalis.it/TSA/tss-usr-gen

# ─── Email (Resend) ───────────────────────────────────────────────────────────
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
EMAIL_FROM=noreply@yousaidthat.org

# ─── App ──────────────────────────────────────────────────────────────────────
NODE_ENV=development
PORT=5000
# Base URL for server-generated links (attestation URLs, emails).
# Falls back to req.protocol + req.host if not set.
APP_URL=https://yousaidthat.org
# Comma-separated list of allowed CORS origins (overrides defaults if set).
# ALLOWED_ORIGINS=https://yousaidthat.org,https://www.yousaidthat.org

# ─── Admin ───────────────────────────────────────────────────────────────────
ADMIN_SECRET=change-me-to-a-random-32-char-string

# ─── Vercel / Supabase Cron (protects /api/cron/* from external calls) ───────
CRON_SECRET=change-me-to-a-random-32-char-string

# ─── Arweave (optional — permanent storage for public predictions) ───────────
# Export your Arweave wallet JSON and paste it here (single line).
ARWEAVE_KEY_JSON=
ARWEAVE_HOST=arweave.net

# ─── Frontend (used by Vite at build time) ────────────────────────────────────
# In development, API is served on the same origin (leave empty).
# In production, set to the backend URL if deploying separately (Railway/Render).
VITE_API_URL=
